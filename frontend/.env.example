# Copy to .env.local only if you need a direct API URL (skip in local dev to use Vite /api proxy).
# VITE_API_URL=http://127.0.0.1:5000
# Optional: where the dev proxy sends /api (default http://127.0.0.1:5000)
# VITE_DEV_API_PROXY=http://127.0.0.1:5000
